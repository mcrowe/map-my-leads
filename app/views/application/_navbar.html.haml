.navbar.navbar-fixed-top
  .navbar-inner
    .container
      = link_to 'MapMyLeads', root_path, class: 'brand'

      %ul.nav
        = nav_item 'Choose an Account', root_path

      %ul.nav.pull-right
        - if @account
          %li
            %a Showing #{@account.name}

        - if signed_in?
          = link_to oauth_logout_path, class: 'btn' do
            %i.icon-off
            Sign Out